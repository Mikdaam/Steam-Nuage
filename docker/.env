# This .env file is added volontarely to the repository because it's used for the docker-compose file.
DB_USER=root
DB_PASSWORD=root
# if you wanna use another remote database, edit the lines below
#DB_DIALECT=
DB_HOST=database
DB_PORT=5432
#DB_NAME=